export const API_BASE_URL = "http://localhost:4002";
export const API_PREFIX = `${API_BASE_URL}/api`;
export const TOKEN_KEY = "skin-helper-token";
export const STATIC_PATH = (filename) => `/static/image/${filename}`;
export const DEFAULT_USER_ID = 1;

export const codingtalkUniToolkitInstall = () => {
  // Keep a no-op hook so the current startup chain stays stable.
};

export default {};
